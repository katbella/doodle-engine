/* Minimal
 * Only the text and choices, with no additional frills.
 */

/* ── Game panels ────────────────────────────────────────────────── */

.panel-workspace {
    position: fixed;
    inset: 0;
    z-index: 100;
    pointer-events: none;
}

.panel {
    position: absolute;
    inset: 0 0 0 12rem;
    overflow: auto;
    padding: 1rem;
    border: 1px solid;
    background: Canvas;
    color: CanvasText;
    border-radius: 0;
    box-shadow: none;
    animation: none;
    pointer-events: auto;
}

.game-menu {
    position: relative;
    z-index: 101;
    display: flex;
    width: 12rem;
    flex-direction: column;
}

.game-menu-icon {
    display: none;
}

/* ── Dialog positioning ─────────────────────────────────────────── */

.travel-confirm-overlay,
.player-setup-overlay,
.pause-menu-overlay {
    position: fixed;
    inset: 0;
    z-index: 100;
    display: grid;
    place-items: center;
    padding: 1rem;
}

.item-modal,
.travel-confirm,
.player-setup,
.pause-menu {
    max-width: min(48rem, 100%);
    max-height: 100%;
    overflow: auto;
    padding: 1rem;
    border: 1px solid;
    background: Canvas;
    color: CanvasText;
}

.panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.shell-panel-overlay {
    position: fixed;
    inset: 0;
    z-index: 100;
}

.shell-panel-overlay .panel {
    inset: 0;
}

.player-setup-overlay {
    background: Canvas;
    color: CanvasText;
}

.panel-body,
.panel-parchment-body,
.panel-parchment-sheet {
    display: flex;
    min-width: 0;
    min-height: 0;
}

.panel-body,
.panel-parchment-body,
.panel-parchment-sheet,
.player-notes-view,
.journal {
    flex: 1;
}

.player-notes-view,
.journal {
    display: flex;
    min-width: 0;
    min-height: 0;
}

.player-notes,
.journal-quests {
    flex: 0 0 40%;
}

.player-notes-list-column,
.journal-entries {
    flex: 1;
    min-width: 0;
}

.player-notes-list {
    list-style: none;
}

.save-slot-thumbnail,
.save-slot-kind,
.save-slot-thumbnail-empty,
.save-slot-kind-empty,
.save-slot-empty-action,
.character-list-icon,
.character-speaking-mark {
    display: none;
}

/* ── Interlude ──────────────────────────────────────────────────── */

.interlude-overlay {
    position: fixed;
    inset: 0;
    z-index: 110;
    overflow: auto;
    padding: 1rem;
    background: Canvas;
    color: CanvasText;
}

.interlude-scroll-container {
    position: relative;
    z-index: 1;
    max-height: calc(100vh - 2rem);
    overflow: auto;
}

/* ── Map positioning ─────────────────────────────────────────────── */

.map-container {
    position: relative;
}

/* ── Accessible hidden labels ────────────────────────────────────── */

.doodle-visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0 0 0 0);
    white-space: nowrap;
    border: 0;
}
